<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hs.dao.DeptDao">
    <select id="getAll" resultType="com.hs.entity.Dept">
        select * from dept
    </select>
</mapper>